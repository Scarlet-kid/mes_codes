import os
import shutil
source = "codage.jpeg"
target = "image/codage.jpeg"
shutil.copy(source,target)
os.remove(source)
#TP
# Depuis le générateur de mot de passe un systeme de stockage des derniers mot de passe.
# Creér une interface graphique pour générer les repas.(lecture+interface graphique).